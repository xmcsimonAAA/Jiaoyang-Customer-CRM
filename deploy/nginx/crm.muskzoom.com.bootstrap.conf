# Temporary HTTP-only configuration for the first Let's Encrypt certificate issuance.
server {
    listen 80;
    listen [::]:80;
    server_name crm.muskzoom.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 404;
    }
}
